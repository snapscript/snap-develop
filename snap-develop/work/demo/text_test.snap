
class GameElement {

    var radius;
    var type;
    var x;
    var y;
    
    new(radius) {
        this.radius = radius;
    }

    newRandomLocation(fieldDimensions, snake) {
      //  var random = new Random();

      //  var valid = false;
      //  while(!valid){
      //       valid = true;

      //       x = random.nextInt(fieldDimensions.x - 2) + 1;
      //       y = random.nextInt(fieldDimensions.y - 2) + 1;

      //       for (var cell in snake.getCells())
      //           if (cell.x == x && cell.y == y)
      //              valid = false;
      //  } 

       // println("New element at: ${x}, ${y}");
    }

    getRadius() {
        return radius;
    }

    getType() {
        return type;
    }
}

class SpecialElement extends GameElement {
   // maximum duration to display clock in snake moves
   static var MAX_DURATION = 0;

   // counter
   var counter;

   var hasExpired;

   new(fieldDimensions, snake, radius, maxDuration): super(radius) {
         newRandomLocation(fieldDimensions, snake);
         MAX_DURATION = maxDuration;
         restartCounter();
   }

   incCounter() {
         counter++;

         if (counter >= MAX_DURATION) {
            hasExpired = true;
         }
   }

   restartCounter() {
         counter = 0;
   }

   hasExpired() {
         return hasExpired;
   }
}
var e = new SpecialElement({x:1,y:1},2,3,4);
println(e++);
