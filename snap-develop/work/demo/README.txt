1) super.<expr> needs to be fixed if a super method calls its super it fails!!
2) stack needs to be done on index for performance and memory (churn) reduction

InnerClassConstructor
InnerTypeName
InnerClassBunder

new(instance, type, ...){

}